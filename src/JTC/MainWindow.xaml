<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="JTC.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:JTC.ViewModels"
    xmlns:tb="using:H.NotifyIcon"
    Title="Junior Torrent Client">
    <Grid x:Name="RootGrid">
        <!-- Background + RequestedTheme are set from code-behind via ThemeHelper based on
             the user's chosen theme (Brand / Dark / Light). -->
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Tray icon — the app minimizes here on window-close instead of quitting,
             so the torrent engine keeps seeding/leeching in the background. -->
        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="Assets/tclient.ico"
                        ToolTipText="Junior Torrent Client"
                        NoLeftClickDelay="True"
                        MenuActivation="RightClick">
            <tb:TaskbarIcon.ContextFlyout>
                <!-- Click= handlers on these items are wired in code-behind (MainWindow ctor).
                     XAML Click= assignment on MenuFlyoutItems inside TaskbarIcon.ContextFlyout
                     doesn't reliably reach the compiled code-behind — the flyout is realized
                     on the shell/notification-area surface, outside the WinUI element tree. -->
                <MenuFlyout>
                    <MenuFlyoutItem x:Name="TrayShowItem" Text="Показать" />
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem x:Name="TrayExitItem" Text="Выход" />
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="32" Background="Transparent">
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="12,0,0,0"
                        Spacing="8">
                <TextBlock Text="Junior Torrent Client"
                           VerticalAlignment="Center"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                <TextBlock x:Name="VersionText"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Padding="12,8">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Left">
                <Button x:Name="OpenTorrentButton" Click="OpenTorrentButton_Click" CornerRadius="16">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE710;" />
                        <TextBlock Text="Открыть .torrent" />
                    </StackPanel>
                </Button>
                <Button x:Name="AddMagnetButton" Click="AddMagnetButton_Click" CornerRadius="16">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE71B;" />
                        <TextBlock Text="Добавить magnet" />
                    </StackPanel>
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="4" HorizontalAlignment="Right">
                <Button x:Name="ResumeButton" Click="ResumeButton_Click"
                        CornerRadius="16"
                        ToolTipService.ToolTip="Возобновить">
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE768;" />
                </Button>
                <Button x:Name="PauseButton" Click="PauseButton_Click"
                        CornerRadius="16"
                        ToolTipService.ToolTip="Пауза">
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE769;" />
                </Button>
                <Button x:Name="RemoveButton" Click="RemoveButton_Click"
                        CornerRadius="16"
                        ToolTipService.ToolTip="Удалить">
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE74D;" />
                </Button>
                <Button x:Name="SettingsButton" Click="SettingsButton_Click"
                        CornerRadius="16"
                        ToolTipService.ToolTip="Настройки"
                        Margin="12,0,0,0">
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE713;" />
                </Button>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="2"
                  x:Name="TorrentList"
                  ItemsSource="{x:Bind ViewModel.Torrents, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedTorrent, Mode=TwoWay}"
                  SelectionMode="Extended"
                  SelectionChanged="TorrentList_SelectionChanged"
                  Tapped="TorrentList_Tapped"
                  Padding="12,0,12,0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <!-- Default ListViewItem constrains its content to Left + adds padding.
                         Stretch so our row Grid gets the full width, and zero padding so
                         our own Grid Padding is the only spacer. -->
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,6" />
                    <!-- WinUI 3 ListViewItem carries a 4 px default CornerRadius (from
                         ControlCornerRadius theme resource) on its own container — that
                         rounds the corners of any child that fills the container, so our
                         plashka Border looks rounded even when its own CornerRadius=0.
                         Force 0 so the Border's own value is the only thing that matters. -->
                    <Setter Property="CornerRadius" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.Resources>
                <!-- Kill the built-in hover/selection overlays. Our per-state row-background
                     is the only visual state indicator; without these overrides, the WinUI
                     hover overlay renders slightly wider than our row Grid and looks weird. -->
                <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver"          Color="Transparent" />
                <SolidColorBrush x:Key="ListViewItemBackgroundPressed"              Color="Transparent" />
                <!-- Container selection background is off; selection is rendered inside
                     our row Grid via TorrentViewModel.IsSelected so the shape matches. -->
                <SolidColorBrush x:Key="ListViewItemBackgroundSelected"             Color="Transparent" />
                <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver"  Color="Transparent" />
                <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPressed"      Color="Transparent" />
            </ListView.Resources>
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <!-- Padding matches the row's inner Grid padding (30,0,20,0) so header
                         columns line up horizontally with the row values below. Vertical
                         padding is 4 top/bottom for breathing room between the header and
                         the toolbar / first row. -->
                    <Grid Padding="30,4,20,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2.5*" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="1.6*" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="60" />
                            <ColumnDefinition Width="60" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <!-- Имя — left-aligned (long text with ellipsis). Размер / Прогресс /
                             Пиры / Сиды — centred so short values (numbers, percentages)
                             sit tidily in their column instead of hugging the left edge.
                             Rate columns (↓ / ↑) stay left because their unit changes width
                             (B/s → KB/s → MB/s) and centring would jitter. The last column
                             carries only a status glyph — no header text needed since the
                             icons are self-explanatory. -->
                        <TextBlock Grid.Column="0" Text="Имя"       Style="{ThemeResource CaptionTextBlockStyle}" />
                        <TextBlock Grid.Column="1" Text="Размер"    Style="{ThemeResource CaptionTextBlockStyle}" HorizontalAlignment="Center" />
                        <TextBlock Grid.Column="2" Text="Прогресс"  Style="{ThemeResource CaptionTextBlockStyle}" HorizontalAlignment="Center" />
                        <TextBlock Grid.Column="3" Text="↓"         Style="{ThemeResource CaptionTextBlockStyle}" />
                        <TextBlock Grid.Column="4" Text="↑"         Style="{ThemeResource CaptionTextBlockStyle}" />
                        <TextBlock Grid.Column="5" Text="Пиры"      Style="{ThemeResource CaptionTextBlockStyle}" HorizontalAlignment="Center" />
                        <TextBlock Grid.Column="6" Text="Сиды"      Style="{ThemeResource CaptionTextBlockStyle}" HorizontalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:TorrentViewModel">
                    <!-- Capsule/pill row — aqua-glass look. CornerRadius large enough (22 for
                         our ~44-tall plashka) that the ends become full semicircles. Border
                         also clips the two overlay Rectangles below so their gloss / shadow
                         gradients curve with the pill outline. Faint 1 px black border adds
                         subtle edge definition on both light and dark plashka colours. -->
                    <Border CornerRadius="{x:Bind PlashkaCornerRadius, Mode=OneWay}"
                            Height="44"
                            BorderThickness="1"
                            BorderBrush="#22000000"
                            Background="{x:Bind RowBackground, Mode=OneWay}"
                            DoubleTapped="TorrentRow_DoubleTapped">
                        <Border.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Открыть" Click="RowOpen_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE838;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Обновить" Click="RowRecheck_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE72C;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutSeparator />
                                <MenuFlyoutItem Text="Удалить" Click="RowDelete_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE74D;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Border.ContextFlyout>
                        <Grid>
                            <!-- Two status indicators; one visible at a time based on
                                 TorrentViewModel.StatusAsCircle (user toggles the style in
                                 Settings). Circle reads better on capsule plashkas; the
                                 4 px stripe is the pre-v0.5.5 shape, better paired with
                                 a square plashka. -->
                            <Ellipse HorizontalAlignment="Left" VerticalAlignment="Center"
                                     Width="8" Height="8" Margin="14,0,0,0"
                                     Fill="{x:Bind StatusBrush, Mode=OneWay}"
                                     Visibility="{x:Bind StatusCircleVisibility, Mode=OneWay}"
                                     IsHitTestVisible="False" />
                            <Rectangle HorizontalAlignment="Left" VerticalAlignment="Stretch"
                                       Width="4"
                                       Fill="{x:Bind StatusBrush, Mode=OneWay}"
                                       Visibility="{x:Bind StatusStripeVisibility, Mode=OneWay}"
                                       IsHitTestVisible="False" />
                            <!-- Top gloss — semi-transparent white gradient covering the upper
                                 half, giving that classic aqua "gel" highlight. Works over any
                                 plashka colour: on white it's nearly invisible (subtle sheen);
                                 on dark or coloured it lifts the top like glass. -->
                            <Rectangle VerticalAlignment="Top" Height="22" IsHitTestVisible="False">
                                <Rectangle.Fill>
                                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="#33FFFFFF" Offset="0" />
                                        <GradientStop Color="#00FFFFFF" Offset="1" />
                                    </LinearGradientBrush>
                                </Rectangle.Fill>
                            </Rectangle>
                            <!-- Bottom shadow — matching darkening on the lower half, so a
                                 light plashka still reads as 3-D (top brighter, bottom darker).
                                 Barely visible on dark plashkas by design. -->
                            <Rectangle VerticalAlignment="Bottom" Height="18" IsHitTestVisible="False">
                                <Rectangle.Fill>
                                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="#00000000" Offset="0" />
                                        <GradientStop Color="#20000000" Offset="1" />
                                    </LinearGradientBrush>
                                </Rectangle.Fill>
                            </Rectangle>
                            <Grid Padding="30,0,20,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2.5*" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="1.6*" />
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="60" />
                                    <ColumnDefinition Width="60" />
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" VerticalAlignment="Center"
                                           Foreground="{x:Bind RowForeground, Mode=OneWay}"
                                           Text="{x:Bind Name, Mode=OneWay}" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                                           Foreground="{x:Bind RowForeground, Mode=OneWay}"
                                           Text="{x:Bind SizeText, Mode=OneWay}" />
                                <TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                                           Foreground="{x:Bind RowForeground, Mode=OneWay}"
                                           Text="{x:Bind ProgressText, Mode=OneWay}" />
                                <TextBlock Grid.Column="3" VerticalAlignment="Center"
                                           Foreground="{x:Bind RowForeground, Mode=OneWay}"
                                           Text="{x:Bind DownloadRateText, Mode=OneWay}" />
                                <TextBlock Grid.Column="4" VerticalAlignment="Center"
                                           Foreground="{x:Bind RowForeground, Mode=OneWay}"
                                           Text="{x:Bind UploadRateText, Mode=OneWay}" />
                                <TextBlock Grid.Column="5" VerticalAlignment="Center" HorizontalAlignment="Center"
                                           Foreground="{x:Bind RowForeground, Mode=OneWay}"
                                           Text="{x:Bind LeecherCount, Mode=OneWay}" />
                                <TextBlock Grid.Column="6" VerticalAlignment="Center" HorizontalAlignment="Center"
                                           Foreground="{x:Bind RowForeground, Mode=OneWay}"
                                           Text="{x:Bind SeedCount, Mode=OneWay}" />
                                <FontIcon Grid.Column="7" VerticalAlignment="Center" HorizontalAlignment="Center"
                                          FontFamily="Segoe MDL2 Assets"
                                          FontSize="16"
                                          Glyph="{x:Bind StateGlyph, Mode=OneWay}"
                                          Foreground="{x:Bind StatusBrush, Mode=OneWay}"
                                          ToolTipService.ToolTip="{x:Bind StateText, Mode=OneWay}" />
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Window>
